- [CEM App Architecture](cem-app-architecture.md) — flat client/server/shared structure; subjects + DB tables defined; sidebar sections config
- [Framer Motion TS Quirks](framer-motion-ts.md) — ease arrays and custom variant functions cause tsc errors; use "easeOut" string + `as any` for custom variants
- [Reports Page Design](reports-design.md) — comprehensive reports page uses two-column layout: left panel with section toggles (10 sections), right main area; KpiCard component uses gradient bg; fetches /api/stats + /api/school + /api/absences + /api/results in parallel
- [UI Beauty Conventions](ui-beauty.md) — bg-dot-grid utility in index.css for subtle background texture; page headers use gradient text + icon badge pattern; KPI cards use bg-gradient-to-br with white/20 radial glow overlay
- [Free LLM via Groq](free-llm-groq.md) — no Replit-managed passthrough billing exists for text/chat LLMs; Groq's free OpenAI-compatible API is the fallback when user wants a no-cost assistant
- [Dev-login test data pollution](dev-login-test-data.md) — /api/dev-login writes real rows under a persistent "dev-test-user" account; clean up after curl-testing an endpoint
- [Timetable & Class Balancer](timetable-classbalancer.md) — new modules: schedule grid, teacher/room CRUD, conflict detection, class balancer with drag-and-drop; WhatsApp button uses school.supportPhone; HourlyVolumePanel added as 5th tab (requires Select import in timetable.tsx)
- [OCR Vision Model](ocr-vision-model.md) — use meta-llama/llama-4-scout-17b-16e-instruct on Groq for vision; GROQ_API_KEY must be set as a secret; absences mode added (type=absences query param); Levenshtein fuzzy name matching in upload-grades-ocr.tsx
- [Algerian Grade Thresholds](algerian-thresholds.md) — passed>=10, mustarrak 9-10, rasib<9; yearend-failed.tsx subtitle must say "10/20" not "9/20" (page shows all <10); /api/students returns {students,total} not array — always unwrap
- [Results Trimestre Filter](results-tri-filter.md) — filters.tri changes displayed avg column; displayed list now sorts by getTriAvg(r, filters.tri) when tri is set; classe filter applied client-side (API does not filter by classe)
